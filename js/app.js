var app = angular.module("myApp", []);

/*app.config(['$routeProvider',
    function($routeProvider) {
        $routeProvider.
            when('/products', {
                templateUrl: 'views/products.html',
                controller: 'MainController'
            }).
            when('/caps', {
                templateUrl: 'views/caps.html',
                controller: 'capController'
            }).
            otherwise({
                redirectTo: '/products'
            });
    }]);*/